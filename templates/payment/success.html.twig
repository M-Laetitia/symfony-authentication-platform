{% extends 'base.html.twig' %}

{% block title %}Payment Success{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>

        .success-page {
            width: 100vw;
            max-width: 500px;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.07);
            text-align: center;
            padding: 4em 1em 2em 1em;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .success-page__icon {
            font-size: 3rem;
            margin-bottom: 1.5rem;
        }
        .success-page__title {
            font-size: 2.2rem;
            font-weight: 700;
            margin-bottom: 0.7em;
            color: var(--color-accent, #2e7d32);
        }
        .success-page__desc {
            color: #444;
            margin-bottom: 2em;
            font-size: 1.1rem;
        }
        .success-page__button {
            display: inline-block;
            margin-top: 1em;
            padding: 0.75em 2em;
            background: var(--color-accent, #2e7d32);
            color: #fff;
            border-radius: 6px;
            font-weight: 600;
            text-decoration: none;
            transition: background 0.2s;
            font-size: 1.1rem;
            box-shadow: 0 2px 8px rgba(46,125,50,0.08);
        }
        .success-page__button:hover {
            background: #256025;
        }
    </style>
{% endblock %}

{% block body %}
    <twig:Banner
        title="Payment Successful!"
        subtitle="Thank you for your order."
        :breadcrumbs="[
            {label: 'Home', url: path('home'), type: 'link'},
            {label: 'separator', type: 'separator' },
            {label: 'Success', type: 'current'}
        ]"
    />
    <div class="success-page">

        <h2 class="success-page__title">Thank you for your trust!</h2>
        <p class="success-page__desc">
            Your order <strong>#{{ orderId }}</strong> is confirmed.<br>
            We’re thrilled to help you capture your special moments.<br>
            Your photographer will contact you soon to discuss the details and make sure everything is perfect.<br>
            <br>
            <span style="color: #2e7d32; font-weight: 500;">Your invoice is on its way by email.</span>
        </p>
        <a class="success-page__button" href="{{ path('invoice_download', { id: invoice.id }) }}" target="_blank">
            Download my invoice
        </a>

    </div>
{% endblock %}